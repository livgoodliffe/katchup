<div class="btn-group container" role="group">
  <% wishlist_active = 'dual-active' if params[:list_type] == 'wishlists' %>
  <% favourite_active = 'dual-active' if params[:list_type] == 'favourites' %>

  <%= link_to "SAVED SPOTS", wishlists_path(list_type: 'wishlists'), :class => "btn btn-basic #{favourite_active}" %>

  <%= link_to "BEEN THERE", favourites_path(list_type: 'favourites'), :class => "btn btn-basic #{wishlist_active}" %>

</div>
